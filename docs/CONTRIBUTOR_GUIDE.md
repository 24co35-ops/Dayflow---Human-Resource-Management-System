# Dayflow Contributor Guide

Use a feature branch, make one coherent change per commit, and run the smallest relevant verification before pushing. Frontend changes belong under `frontend/src`; API changes belong under `backend/app`; migrations belong under `supabase/migrations`; and decisions belong under `docs`. Do not commit `.env` secrets, generated caches, or fake authorship.
