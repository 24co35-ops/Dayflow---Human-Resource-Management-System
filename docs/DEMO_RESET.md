# Dayflow Demo Reset

Refresh the browser, return to the root route, select Employee view, and confirm that the initial seeded queue is visible. Open Flow and use the prompt chip rather than typing sensitive information. After demonstrating approval, reload to restore the seeded offline state or open a fresh browser context. The demo never depends on real employee data.
