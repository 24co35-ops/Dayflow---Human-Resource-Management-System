# Dayflow Mobile Layout

On narrow screens, the sidebar collapses into the top brand row, metric cards become a single-column stack, pulse rows retain identity and status, and people tables scroll horizontally. Flow remains a bottom sheet-width panel with an accessible composer. Approval buttons stay visible even when drag-and-drop is unavailable.
