# Dayflow Performance Notes

The command center keeps the first screen focused on the demo-critical payload. Tables should paginate or virtualize as the employee directory grows. Realtime updates should patch the relevant cache rather than reload the entire dashboard. Flow history should be bounded in the browser, and payslip generation should remain a user-triggered action.
