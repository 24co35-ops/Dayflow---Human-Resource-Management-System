# Dayflow Leave Rules

Leave requests use Paid, Sick, or Unpaid types and move through Pending, Approved, or Rejected states. Dates are inclusive and the end date cannot precede the start date. Employees create requests for themselves. HR/Admin performs the review transition and can add a comment. Overlapping pending or approved requests should be rejected before production release.
